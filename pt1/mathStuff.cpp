#include <iostream>
    // Include the cmath library
#include <cmath>
using namespace std;

int main() {


    cout << sqrt(64);
    cout << round(2.6);
    cout << log(2);

    int xx = 10;
    int Yy = 9;
    cout << (xx > Yy); // returns 1 (true), because 10 is higher than 9

    int xxx = 10;
    cout << (xxx == 10);  // returns 1 (true), because the value of x is equal to 10



    if (20 > 18) {
        cout << "20 is greater than 18";
    }

    int x = 20;
    int y = 18;
    if (x > y) {
        cout << "x is greater than y";
    }


}